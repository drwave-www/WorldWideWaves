<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!--
        Test manifest for androidInstrumentedTest.
        Registers test-only activities used by instrumented tests.
    -->

    <application>
        <!--
            TestMapActivity: Minimal activity for MapView integration tests.
            Note: Do not specify android:exported="false" or intent-filters here.
            ActivityScenarioRule will launch this activity directly for testing.
        -->
        <activity
            android:name="com.worldwidewaves.map.TestMapActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
    </application>

</manifest>
