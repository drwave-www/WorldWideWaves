<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme -->
    <style name="Theme.WorldWideWaves" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color -->
        <item name="colorPrimary">@android:color/black</item>
        <item name="colorPrimaryDark">@android:color/black</item>
        <item name="colorAccent">@android:color/white</item>
    </style>

    <!-- Splash Screen theme -->
    <style name="Theme.WorldWideWaves.Splash" parent="Theme.SplashScreen">
        <!-- Set the splash screen background to match your current background -->
        <item name="windowSplashScreenBackground">@android:color/black</item>
        
        <!-- Use the logo as the splash screen icon -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/www_logo_transparent</item>
        
        <!-- Configure animation duration -->
        <item name="windowSplashScreenAnimationDuration">50</item>
        
        <!-- Set the theme used after splash screen appears -->
        <item name="postSplashScreenTheme">@style/Theme.WorldWideWaves</item>
        
        <!-- Status bar and navigation bar configurations -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>
