// EventFullMapView.swift - Deprecated, use shared Compose implementation
// All functionality is now in shared FullMapScreen

import SwiftUI

struct EventFullMapView: View {
    var body: some View {
        Text("EventFullMapView deprecated - use shared Compose")
    }
}