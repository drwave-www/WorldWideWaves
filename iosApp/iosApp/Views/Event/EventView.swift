// EventView.swift - Deprecated, use shared Compose implementation
// All functionality is now in shared StandardEventLayout

import SwiftUI

struct EventView: View {
    var body: some View {
        Text("EventView deprecated - use shared Compose")
    }
}