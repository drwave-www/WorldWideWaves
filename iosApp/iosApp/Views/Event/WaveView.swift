// WaveView.swift - Deprecated, use shared Compose implementation
// All functionality is now in shared WaveScreen

import SwiftUI

struct WaveView: View {
    var body: some View {
        Text("WaveView deprecated - use shared Compose")
    }
}