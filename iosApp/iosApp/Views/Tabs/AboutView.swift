// AboutView.swift - Deprecated, use shared Compose implementation
// All functionality is now in shared AboutScreen

import SwiftUI

struct AboutView: View {
    var body: some View {
        Text("AboutView deprecated - use shared Compose")
    }
}