// SettingsView.swift - Deprecated, use shared Compose implementation
// All functionality is now in shared DebugScreen

import SwiftUI

struct SettingsView: View {
    var body: some View {
        Text("SettingsView deprecated - use shared Compose")
    }
}